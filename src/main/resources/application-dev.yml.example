spring:
  datasource:
    # Supabase (dev, pooled): set DB_PASSWORD to your Supabase password
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres}
    username: ${DB_USER:postgres.lrfzljpdxzgouhmzayxc}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true


  flyway:
    enabled: false

server:
  port: 8080

logging:
  level:
    com.moneyflow: DEBUG
